// Find the first element in the array numbers = [10, 20, 30, 40] that is greater than 25.

const numbers = [10, 20, 30, 40];
const first_index = numbers.findIndex(item => item > 25);
console.log(first_index);