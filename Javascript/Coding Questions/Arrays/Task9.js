// From the array ages = [25, 18, 12, 30, 15], create a new array containing only ages greater than or equal to 18.

const ages = [25, 18, 12, 30, 15];
const ages_filter = ages.filter(num => num > 18);
console.log(ages_filter)